<% provide(:title, "Sign Up") %>
<h1>New User Sign Up</h1>

<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <%= render 'shared/error_messages' %>
  <% end %>
  <%= render 'fields', f: f %>
  <%= f.submit "Create my account" %>
<% end %>
